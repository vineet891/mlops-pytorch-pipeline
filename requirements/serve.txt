# Inference-only dependencies. Training extras such as tensorboard
# stay out of this file so the serving image stays smaller.
torch==2.2.2
torchvision==0.17.2
PyYAML==6.0.2
fastapi==0.115.6
uvicorn==0.34.0
pillow==11.1.0
python-multipart==0.0.20
